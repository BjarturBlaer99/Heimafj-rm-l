# Web production release — 18 September 2026

Status: deployed and verified.

- Website: https://minfjarmal.vercel.app
- Application commit: `a184a54` — `Polish finance web app and harden production data workflows`.
- GitHub `main` was fast-forwarded to this commit; the release branch is `codex/web-release-2026-09-18`.
- Vercel project: `supabase-personal-finance` / `prj_jEnELVbtwTa7aorc05pEwI4KHjdD`.
- Deployment: `dpl_BEHs3cU2ybWnQmwFX2jy8JWaxz4U`.
- Deployment URL: https://supabase-personal-finance-8jcdkrckv-bjarturs-projects-24b5a003.vercel.app
- Runtime: Node.js 22.x, pinned in the application and lockfile metadata.

The candidate was built with production settings using `--skip-domain`, checked while protected, then promoted after the database migrations succeeded. Local environment files, logs, dependencies, build output and the mobile app were excluded from the upload. Only the web release was committed.

## Database changes

The production Supabase project was verified in the dashboard as **Finance app**, reference `gtgqxgddlqcvspegvxzt`, running PostgreSQL 17.6.

Applied, in order:

1. `supabase/bill-payment-integrity-update.sql`
2. `supabase/savings-contribution-integrity-update.sql`

Preflight confirmed zero duplicate expense links, zero mismatched bill/expense links, required functions/indexes/table privileges, and the expected ownership policies on all five relevant RLS-enabled tables. Both transactional migrations completed successfully.

Post-migration read-only verification returned ten passing checks: the four new function bodies matched the submitted SQL with the expected security modes/search paths/ACLs, both internal signup helpers had restricted execution, all three integrity triggers were enabled with the correct events/functions, and the unique expense-link index was valid. PostgREST's schema cache was notified to reload. No real transaction, savings balance, account or bill record was created, edited or deleted for testing.

The isolated PostgreSQL 17.10 suite passed **33 checks**, including Supabase-style default grants, restricted seed helpers, signup, RLS, concurrent writes, rollback and safe retries. These tests used synthetic accounts outside production.

## Verification

- 138 application tests passed; lint, TypeScript and optimized build passed.
- Both dependency audits reported zero known vulnerabilities in the checked lockfile.
- Vercel production build reached `Ready` before promotion.
- The live `/`, `/login`, `/help` and `/privacy` routes returned HTTP 200.
- Anonymous `/api/data/export` returned HTTP 401 as expected.
- Production CSP excludes `unsafe-eval`.
- The live homepage displayed the new design and content. The live demo opened the shared dashboard without captured browser errors or warnings.
- Local production-build mobile checks covered 320/390-pixel layouts and savings retry behavior. This was not a physical iPhone Safari test or a financial write test against a real production account.

## Operations and rollback

The previous healthy production deployment was `dpl_BBpAh7Li92P8fytwR37eLvXXV7PM` at https://supabase-personal-finance-hys9hgz94-bjarturs-projects-24b5a003.vercel.app . Preserve the database integrity protections if application rollback is needed; older application behavior must be reviewed before reverting.

`SUPPORT_EMAIL` remains unset. Help uses its existing fallback; deletion requests still require the manual operator workflow in `docs/account-data-operations.md`. No automatic deletion or notification worker was introduced.
